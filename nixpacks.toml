[phases.setup]
aptPkgs = ["libexpat1"]
nixPkgs = ["python312", "poetry"]

[phases.install]
cmds = ["poetry install --no-interaction --no-ansi --only main"]

[start]
cmd = "poetry run uvicorn backend.app:app --host 0.0.0.0 --port $PORT"